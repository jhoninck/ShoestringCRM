# Extended Platform Dependency Matrix

| Subsystem / Domain | PostgreSQL | MinIO | NATS | Redis | Purpose of Each Integration |
|---|---|---|---|---|---|
| VoIP / Telephony | ✅ | ✅ | ✅ | ✅ | PostgreSQL for CDR, extensions, routing, provisioning. MinIO for recordings, voicemail, prompts. NATS for call events, analytics, downstream workflows. Redis for SIP registration, presence, transient session/cache data. |
| Email / Mail Platform | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for accounts, folders, metadata, delivery status. MinIO for attachments and archived MIME content. NATS for inbound/outbound processing, spam events, notifications. Redis for throttling, queue support, temporary delivery state. |
| Social Media Core | ✅ | ✅ | ✅ | ✅ | PostgreSQL for posts, profiles, relationships, metadata. MinIO for images, videos, media assets. NATS for post events, moderation, fan-out, notifications. Redis for feed caching, sessions, counters, hot aggregates. |
| Chat / Direct Messaging | ✅ | ✅ | ✅ | ✅ | PostgreSQL for conversation metadata, membership, delivery state. MinIO for media attachments, voice notes, shared files. NATS for message events and realtime delivery pipelines. Redis for presence, websocket/session state, unread counters, ephemeral caches. |
| Group / Community Management | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for group metadata, membership, permissions. MinIO for banners, avatars, attached files. NATS for membership changes, moderation signals, notification fan-out. Redis for cached group views and access/session acceleration. |
| GraphQL / Hypergraph API | ✅ | ⚪ | ✅ | ✅ | PostgreSQL as primary data backend. MinIO only when exposing binary-backed assets through API. NATS for subscriptions, async resolvers, event propagation. Redis for query caching, session tokens, response acceleration. |
| REST / Edge API Gateway | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for client/app registrations, API metadata, rate-plan info. NATS for async job delegation and domain events. Redis for rate limiting, token/session lookups, response cache, request throttling. |
| Identity / Access Management | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for users, roles, auth metadata, audit records. MinIO optionally for identity documents or exported audit bundles. NATS for auth events, provisioning, audit streaming. Redis for session/token cache, login throttling, short-lived authorization data. |
| SSO / OIDC Provider | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for clients, users, grants, consent records. NATS for login/audit events and provisioning hooks. Redis for session persistence, token cache, revocation cache, transient authentication state. |
| CRM Contacts | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for contact records, account relationships, lead history. MinIO optionally for imported vCards or attached files. NATS for lead updates, sync events, workflow triggers. Redis for cached views and quick lookup acceleration. |
| CRM Accounts / Organizations | ✅ | ⚪ | ✅ | ⚪ | PostgreSQL for company records, hierarchies, contracts, ownership. NATS for account lifecycle events, integration sync. Redis optionally for hot account caches. |
| CRM Opportunities / Sales Pipeline | ✅ | ⚪ | ✅ | ⚪ | PostgreSQL for pipeline stages, deals, forecasts, sales metadata. NATS for stage changes, alerts, automation triggers. Redis optionally for dashboard acceleration and temporary cache. |
| Customer Support / Ticketing | ✅ | ✅ | ✅ | ✅ | PostgreSQL for tickets, comments, assignments, status history. MinIO for screenshots, attachments, exported cases. NATS for ticket events, escalations, SLA workflows. Redis for queue cache, agent presence, fast counters. |
| Knowledge Base / Help Center | ✅ | ✅ | ✅ | ✅ | PostgreSQL for articles, versions, categories, permissions. MinIO for article assets, PDFs, attachments. NATS for indexing, publish events, recommendation workflows. Redis for cache, hot search snippets, rendered page acceleration. |
| Document Management | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for metadata, ACLs, version history. MinIO for document binaries. NATS for indexing, OCR, workflow, classification. Redis optionally for access/session cache. |
| File Storage / User Uploads | ✅ | ✅ | ✅ | ✅ | PostgreSQL for ownership metadata, quotas, ACLs. MinIO as primary object storage. NATS for upload-complete, scanning, transcoding, lifecycle processing. Redis for signed URL cache, upload session state, hot metadata. |
| Media Processing / Transcoding | ✅ | ✅ | ✅ | ✅ | PostgreSQL for job metadata, processing history, media catalog. MinIO for source and output media. NATS for pipeline orchestration and job events. Redis for work queue assistance, temporary job state, dedupe cache. |
| Video Streaming / VOD | ✅ | ✅ | ✅ | ✅ | PostgreSQL for catalog, entitlements, playlists, watch history metadata. MinIO for video segments, thumbnails, subtitles. NATS for ingest/transcode/publish events. Redis for edge cache coordination, sessions, view counters, hot manifests. |
| Live Streaming | ✅ | ✅ | ✅ | ✅ | PostgreSQL for stream metadata, scheduling, channel ownership. MinIO for recordings, clips, thumbnails. NATS for live events, moderation, chat integration, analytics. Redis for presence, stream state, ephemeral counters, fan-out support. |
| Audio / Podcast Platform | ✅ | ✅ | ✅ | ✅ | PostgreSQL for episodes, channels, subscriptions, metadata. MinIO for audio files, waveforms, artwork. NATS for publish events, processing, analytics. Redis for feed cache, counters, hot lookups. |
| Notifications Service | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for templates, history, user preferences. MinIO optionally for notification assets. NATS for fan-out and delivery triggers. Redis for deduplication, rate limiting, pending delivery cache. |
| Search / Indexing | ✅ | ✅ | ✅ | ✅ | PostgreSQL for indexing metadata, relevance settings, source references. MinIO for raw documents or extracted corpora. NATS for indexing triggers and refresh events. Redis for query cache, autocomplete cache, hot search fragments. |
| Recommendation Engine | ✅ | ✅ | ✅ | ✅ | PostgreSQL for recommendation metadata, feature definitions, user/item relations. MinIO for datasets, model artifacts, embeddings snapshots. NATS for event ingestion and online feature updates. Redis for low-latency feature cache and serving cache. |
| AI Inference Gateway | ✅ | ✅ | ✅ | ✅ | PostgreSQL for model registry, prompts, usage logs, billing metadata. MinIO for model files, prompt corpora, output artifacts. NATS for async inference jobs and workflow chaining. Redis for prompt/result cache, rate limiting, short-lived session state. |
| AI Training Pipeline | ✅ | ✅ | ✅ | ✅ | PostgreSQL for experiment metadata, lineage, labels, run records. MinIO for datasets, checkpoints, trained models. NATS for pipeline coordination, training events. Redis for job coordination cache and ephemeral worker state. |
| Moderation / Trust & Safety | ✅ | ✅ | ✅ | ✅ | PostgreSQL for cases, policies, rule metadata, sanctions, review logs. MinIO for evidence files, flagged media, exports. NATS for moderation events and downstream enforcement. Redis for hot policy cache, dedupe, realtime rule checks. |
| Fraud / Abuse Detection | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for fraud cases, signals, policy decisions, histories. NATS for signal ingestion and detection events. Redis for scoring cache, realtime counters, abuse throttling. |
| Billing / Payments | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for invoices, subscriptions, ledger metadata, payment state. NATS for payment events, provisioning, reconciliation triggers. Redis for idempotency keys, checkout/session state, rate limiting. |
| Subscription Management | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for plans, entitlements, renewals, account state. NATS for lifecycle changes and provisioning signals. Redis for entitlement cache and checkout/session speedups. |
| Product Catalog / Offers | ✅ | ✅ | ✅ | ✅ | PostgreSQL for products, pricing, variants, taxonomy. MinIO for product images/assets. NATS for catalog update events and sync jobs. Redis for hot product cache and search/filter acceleration. |
| Order Management | ✅ | ✅ | ✅ | ✅ | PostgreSQL for orders, fulfillment state, line items, audit history. MinIO for invoices, shipment docs, attachments. NATS for order lifecycle events and integration flows. Redis for cart/session support and hot order cache. |
| Inventory / Asset Tracking | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for stock levels, asset metadata, movement history. NATS for stock change events and sync flows. Redis for reservation cache, hot availability lookups, contention control. |
| Workflow / Automation Engine | ✅ | ⚪ | ✅ | ⚪ | PostgreSQL for workflow definitions, execution history, state persistence. NATS for triggers, async tasks, event-driven transitions. Redis optionally for short-lived locks and temporary execution cache. |
| Scheduler / Jobs / Background Tasks | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for durable schedules and run history. NATS for distributed execution and job dispatch. Redis for locks, leader election support, ephemeral worker state. |
| Analytics / Event Processing | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for aggregated metrics, dimensional models, dashboard data. MinIO for raw event archives and cold storage. NATS for event ingestion. Redis optionally for hot dashboard cache and counters. |
| Audit Logging / Compliance | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for structured audit trails and searchable compliance metadata. MinIO for long-term archives, evidence bundles, exports. NATS for audit event distribution. Redis optionally for short-lived compliance workflow cache. |
| Federation / ActivityPub | ✅ | ✅ | ✅ | ✅ | PostgreSQL for actors, objects, inbox/outbox metadata, delivery logs. MinIO for media assets and fetched remote content. NATS for federation events and retries. Redis for inbox/outbox queues, cache, rate control. |
| Webhooks / External Integrations | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for endpoint registry, delivery logs, retry metadata. NATS for outbound event fan-out. Redis for retry scheduling cache, dedupe, backoff state. |
| Import / Export Service | ✅ | ✅ | ✅ | ✅ | PostgreSQL for import metadata, mappings, progress records. MinIO for source files and generated exports. NATS for job orchestration and progress events. Redis for temporary progress cache and chunk coordination. |
| Reporting / BI Exports | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for report definitions, generated metadata, structured results. MinIO for exported CSV/PDF bundles. NATS for report generation triggers and delivery workflows. Redis optionally for report result caching. |
| CMS / Content Management | ✅ | ✅ | ✅ | ✅ | PostgreSQL for pages, content models, localization, publish states. MinIO for media, documents, theme assets. NATS for publishing, indexing, cache invalidation events. Redis for rendered page cache and preview/session state. |
| Blog / Newsletter Platform | ✅ | ✅ | ✅ | ✅ | PostgreSQL for posts, subscribers, campaigns, issue metadata. MinIO for images, attachments, generated archives. NATS for send events, publish workflows, analytics updates. Redis for subscriber cache, rate limiting, hot content cache. |
| Ads / Campaign Management | ✅ | ✅ | ✅ | ✅ | PostgreSQL for campaigns, targeting metadata, budgets, creatives metadata. MinIO for creative assets. NATS for impression/click/conversion events and pacing workflows. Redis for budget counters, frequency caps, hot targeting cache. |
| Feed / Timeline Service | ✅ | ✅ | ✅ | ✅ | PostgreSQL for source-of-truth feed metadata and ranking inputs. MinIO for referenced media objects. NATS for post/update/follow events. Redis for assembled timelines, ranking cache, cursor state, hot fan-out data. |
| Presence / Realtime State | ⚪ | ❌ | ✅ | ✅ | PostgreSQL only optionally for snapshots or analytics, not main runtime state. NATS for presence events and fan-out. Redis for primary online/offline, typing, ephemeral channel state. |
| Voice / Video Conferencing | ✅ | ✅ | ✅ | ✅ | PostgreSQL for rooms, participants, scheduling, call metadata. MinIO for recordings, snapshots, uploaded assets. NATS for call/control events and integration hooks. Redis for presence, ephemeral room state, signaling assistance. |
| IoT / Device Registry | ✅ | ✅ | ✅ | ✅ | PostgreSQL for device registry, ownership, firmware metadata. MinIO for firmware binaries and logs/artifacts. NATS for telemetry and command events. Redis for current device shadow/cache and command dedupe. |
| Mobile Push / Device Messaging | ✅ | ⚪ | ✅ | ✅ | PostgreSQL for device tokens, push preferences, delivery history. NATS for push trigger events. Redis for token cache, rate limiting, dedupe, pending delivery state. |
| Geospatial / Location Features | ✅ | ✅ | ✅ | ✅ | PostgreSQL for geospatial records and queries. MinIO for map tiles, exports, geodata files. NATS for location events and async processing. Redis for hot geo caches and proximity results. |
| Forms / Surveys | ✅ | ✅ | ✅ | ✅ | PostgreSQL for forms, questions, responses, permissions. MinIO for attachments and exports. NATS for submission events and follow-up workflows. Redis for draft/session cache and hot form rendering. |
| E-signature / Contract Workflow | ✅ | ✅ | ✅ | ✅ | PostgreSQL for contracts, signer metadata, status history. MinIO for signed PDFs and evidence files. NATS for signature events and workflow orchestration. Redis for signing session state and short-lived validation cache. |
| Backup / Archive Catalog | ✅ | ✅ | ✅ | ⚪ | PostgreSQL for backup inventory, retention policies, restore metadata. MinIO for archived backup objects and export bundles. NATS for backup/restore events. Redis optionally for operational cache. |

## Legend

- ✅ = primary or common dependency
- ⚪ = optional / contextual dependency
- ❌ = generally not recommended as a primary dependency
